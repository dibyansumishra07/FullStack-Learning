package problem_solving.design_patterns.STRUCTURAL.ADAPTER;

public interface Pen {
    // Adapter interface should have the same methods as the adaptee interface
    void write(String assignment);
}
